if GlobalState.get_flag("ROBOT_BATTERY_GET") == false:
	The silly robot won't mind if I take this.
	do GlobalState.set_flag("ROBOT_BATTERY_GET")
	jump battery_collect/
else:
	Now you're even less silly...